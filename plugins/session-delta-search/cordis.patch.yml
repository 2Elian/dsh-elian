# Bundle patch for @2elian/dsh-client-ui-session-delta-search.
# Selecting this bundle inserts one profile row. `plugin_manager`
# (action: install_bundle) reads this file through `dsh.bundle.patch`.
- insert:
    - id: session-delta-search
      name: '@2elian/dsh-client-ui-session-delta-search'
